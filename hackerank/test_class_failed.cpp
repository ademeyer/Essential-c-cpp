#include <bits/stdc++.h>
using namespace std;

bool is_number(string val)
{
  return true;
}

template <typename T>
class SomeClass
{
public:
  void addValue(const T &val)
  {
  }

  T getMax()
  {
  }

  T getValue(const T &val)
  {
    if (is_number(val))
    {
    }
  }
};